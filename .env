# Example .env (dummy values). Do NOT store real secrets here.
# These values are safe placeholders for local testing only.
GITLAB_API_URL="https://gitlab.com/api/v4"
GITLAB_TOKEN="dummy-token-change-me"
GITLAB_USERNAME="john.doe"
# Optional: comma-separated list. If set, takes precedence over GITLAB_USERNAME in fetcher
GITLAB_ASSIGNEES="alice,bob,charlie"